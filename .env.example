DB_NAME=frenchpress
DB_USERNAME=frenchpress
DB_PASSWORD=change-me
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SPRING_PROFILES_ACTIVE=dev
# Comma-separated extra hostnames the Angular SSR server will accept requests
# for, beyond localhost/frenchpress.zabrown.com — needed if you're testing
# from another device on your LAN via the server's IP.
ALLOWED_HOSTS=
